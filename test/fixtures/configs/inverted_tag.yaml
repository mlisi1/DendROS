groups:
  nav:
    color: "bold green"
    label: "NAV"
    nodes:
      - nav2_controller

  hardware:
    color: "dark yellow"
    label: "HW"
    tag_style: inverted
    nodes:
      - robot_state_publisher

defaults:
  show_group_tag: true
  tag_style: inverted
